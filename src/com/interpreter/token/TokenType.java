package com.interpreter.token;

public enum TokenType {
    PLUS,
    MINUS,
    MULTIPLY,
    DIVISION,
    EQUAL,
    LT,
    GT,
    EOF,

    NUMBER,
    WORD,
    IF,
    ELSE,
    OPEN_BRACKET,
    CLOSE_BRACKET
}
